<?php
// =========================================================================================
// File: config.php
// Path: /config.php (در ریشه پروژه)
// Description: فایل اصلی پیکربندی ربات ddkate.
// این فایل توسط اسکریپت نصب به صورت خودکار ساخته می‌شود.
// مقادیر زیر صرفاً به عنوان نمونه هستند و شما نباید آن‌ها را دستی تغییر دهید.
// =========================================================================================

// --- Database Configuration | تنظیمات دیتابیس ---
// این مقادیر توسط اسکریپت نصب (install.sh) پر می‌شوند.

/**
 * آدرس میزبان دیتابیس (معمولاً localhost است).
 */
define('DB_HOST', 'localhost');

/**
 * نام دیتابیس شما.
 */
define('DB_NAME', 'ddkate_bot_db');

/**
 * نام کاربری برای اتصال به دیتابیس.
 */
define('DB_USER', 'ddkate_user');

/**
 * رمز عبور برای اتصال به دیتابیس.
 */
define('DB_PASS', 'your_secure_database_password');


// --- Telegram Bot Configuration | تنظیمات ربات تلگرام ---
// این مقادیر توسط اسکریپت نصب (install.sh) از شما دریافت می‌شوند.

/**
 * توکن ربات که از BotFather دریافت کرده‌اید.
 */
define('BOT_TOKEN', 'YOUR_TELEGRAM_BOT_TOKEN');

/**
 * آیدی عددی ادمین اصلی ربات.
 */
define('ADMIN_ID', 'YOUR_ADMIN_CHAT_ID');

/**
 * نام کاربری ربات شما (بدون @).
 * این مقدار برای ساخت لینک دعوت و موارد دیگر استفاده می‌شود.
 */
define('BOT_USERNAME', 'YourBotUsername');


// --- Webhook and Domain Configuration | تنظیمات دامنه و وبهوک ---

/**
 * آدرس کامل وبهوک ربات شما.
 * این آدرس باید به فایل index.php اصلی در سرور شما اشاره کند.
 * این مقدار توسط اسکریپت نصب (install.sh) به صورت خودکار تنظیم می‌شود.
 */
define('WEBHOOK_URL', 'https://your.domain.com/index.php');


// --- General Settings | تنظیمات عمومی ربات ---

/**
 * زبان پیش‌فرض ربات. در حال حاضر 'fa' (فارسی) تنظیم شده است.
 * در آینده می‌توانید با افزودن فایل‌های زبان دیگر، این مقدار را تغییر دهید.
 */
define('DEFAULT_LANG', 'fa');

/**
 * آیدی تلگرام شما برای نمایش در بخش پشتیبانی یا تبلیغات.
 */
define('SUPPORT_ID', 'parsamoradi199');

?>
